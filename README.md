# tiritara-privacy

tiritara が公開するアプリのプライバシーポリシー。

- 本体: https://kagen.github.io/tiritara-privacy/
- アプリ名を出す場合: `?app=アプリ名`

収集内容は静的 HTML に書いてある。`?app=` は見出しの表示名を差し替えるだけで、
JavaScript を実行しない閲覧者・審査ツールでも全文が読める。

旧 URL `kagen.github.io/kattalyst-privacy/` は既存アプリが掲載しているため、
転送ページとして残してある。
